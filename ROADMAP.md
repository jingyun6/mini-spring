# mini-spring 学习路线图

本文件用于规划 mini-spring 的整体学习进度、版本里程碑和验收标准。README 负责描述
当前能力，CHANGELOG 负责记录已经发生的版本变化，本文件负责回答“下一步做什么”和
“距离完整目标还有多远”。

## 进度快照

| 项目 | 当前状态 |
|---|---|
| 当前版本 | v0.5.0 |
| 当前阶段 | v0.6.0 进行中：字段与构造器参数 `@Qualifier` 精确选择完成 |
| 已完成里程碑 | 5 / 10 |
| 里程碑完成度 | 50% |
| 工作量加权进度 | 40% |
| 自动化测试 | 45 个，全部通过 |
| 下一版本 | v0.6.0 — 依赖选择整合 |

里程碑完成度按版本数量计算；工作量加权进度根据后续生命周期、三级缓存和 AOP 的
复杂度估算，更适合表示整体学习项目的真实进度。

## 总体里程碑

| 版本 | 里程碑 | 权重 | 状态 | 累计进度 |
|---|---|---:|---|---:|
| v0.1.0 | 基础 IoC 主链路 | 20% | 已完成 | 20% |
| v0.2.0 | 循环依赖检测 | 5% | 已完成 | 25% |
| v0.3.0 | 单例并发唯一创建 | 5% | 已完成 | 30% |
| v0.4.0 | 基础构造器注入 | 5% | 已完成 | 35% |
| v0.5.0 | 显式构造器选择与 Primary 候选 | 5% | 已完成 | 40% |
| v0.6.0 | `@Primary` 扫描与 `@Qualifier` 精确选择 | 5% | 进行中 | 40% |
| v0.7.0 | Bean 生命周期与扩展点 | 15% | 未开始 | 40% |
| v0.8.0 | 三级缓存与早期 Bean 引用 | 15% | 未开始 | 40% |
| v0.9.0 | JDK 动态代理与 AOP | 15% | 未开始 | 40% |
| v0.10.0 | Environment、资源与事件 | 10% | 未开始 | 40% |

## 已完成阶段

### v0.1.0 — 基础 IoC 主链路

- `@Component` 文件目录类路径扫描
- BeanDefinition 注册、查询和顺序快照
- 单例一级缓存
- `ApplicationContext` 扫描、注册和预实例化
- 按名称、具体类型、接口和父类获取 Bean
- `@Autowired` 私有字段及父类字段注入
- 缺失定义、多候选和创建失败异常体系

### v0.2.0 — 循环依赖检测

- 使用线程级有序路径记录 Bean 创建调用链
- 检测直接自依赖和多个 Bean 形成的闭环
- 异常信息展示完整路径，例如 `a -> b -> a`
- 使用 `finally` 清理路径，失败后可以安全重试

### v0.3.0 — 单例并发唯一创建

- 单例缓存锁外快速查询
- BeanFactory 级创建监视器
- 锁内二次查询避免等待线程重复创建
- 并发请求只实例化一次并返回同一引用
- prototype Bean 绕过单例创建监视器

### v0.4.0 — 基础构造器注入

- 唯一构造器的隐式注入
- 多构造器场景的无参回退
- 构造器参数按类型解析
- 构造器歧义的诊断异常
- 实例化拆分为选择、参数解析和反射调用

### v0.5.0 — 显式构造器选择与 Primary 候选

- 多构造器中的唯一 `@Autowired` 显式选择
- 多个 `@Autowired` 构造器的冲突检测
- `@Primary` 注解契约和 BeanDefinition primary 元数据
- 唯一 primary 候选选择与多 primary 诊断
- 语义化版本标签发布脚本

## 后续阶段

### v0.6.0 — 依赖选择整合

目标：打通注解配置到候选决策的完整链路，并为字段与构造器参数提供统一的精确候选选择。

课程拆分：

1. [x] 将组件类上的 `@Primary` 映射为 BeanDefinition 元数据
2. [x] 定义 `@Qualifier` 字段与构造器参数注入点契约
3. [x] 实现字段 `@Qualifier` 精确候选选择
4. [x] 实现构造器参数 `@Qualifier` 精确候选选择
5. [ ] 统一字段与构造器参数的依赖描述和候选解析边界
6. [ ] 回归原有注入、单例并发和循环检测，发布 v0.6.0

验收标准：

- 扫描组件上的 `@Primary` 能够影响按类型查询和依赖注入
- 多候选依赖可以通过 `@Primary` 或 `@Qualifier` 唯一确定
- 字段与构造器参数复用同一套候选解析规则
- 原有字段注入、单例并发和循环检测测试全部通过

### v0.7.0 — Bean 生命周期与扩展点

目标：把“创建对象”拆分为可扩展、可观察的生命周期阶段。

课程拆分：

1. 定义实例化、属性填充、初始化和销毁阶段
2. 实现 `BeanPostProcessor` 注册与有序调用
3. 增加初始化和销毁回调
4. 让 ApplicationContext 管理关闭流程并发布 v0.7.0

验收标准：

- 后处理器能够在初始化前后替换 Bean
- 初始化和销毁回调各执行一次
- 多个后处理器调用顺序稳定
- 创建失败时不留下不完整单例

### v0.8.0 — 三级缓存与早期 Bean 引用

目标：在保持单例语义的前提下，解决可以安全暴露早期引用的字段循环依赖。

课程拆分：

1. 拆分完整单例、早期单例和单例工厂三级缓存
2. 在实例化后、属性填充前暴露早期引用
3. 解决 singleton 字段循环依赖，继续拒绝 prototype 和构造器循环依赖
4. 验证异常清理、并发一致性和缓存迁移并发布 v0.8.0

验收标准：

- `A -> B -> A` 的 singleton 字段依赖能够获得同一对象引用
- 构造器循环依赖仍以清晰路径失败
- 创建失败后三级缓存不残留对象或工厂
- 并发获取不会暴露多个早期实例

### v0.9.0 — JDK 动态代理与 AOP

目标：基于 Bean 生命周期扩展点，为接口类型 Bean 创建透明代理。

课程拆分：

1. 理解 JDK 动态代理和方法调用链
2. 定义方法拦截器、通知和切点匹配契约
3. 实现代理工厂与拦截器链
4. 通过 BeanPostProcessor 自动包装匹配的 Bean
5. 验证代理后的依赖注入、单例身份和异常传播并发布 v0.9.0

验收标准：

- 匹配方法能够执行前置、后置和异常通知
- 未匹配方法保持原始行为
- 代理对象仍遵守容器单例语义
- 多个拦截器顺序稳定且可测试

### v0.10.0 — Environment、资源与事件

目标：补充应用级基础设施，使容器可以加载外部配置并进行松耦合事件通信。

课程拆分：

1. 定义 Resource 抽象并支持 classpath、文件资源
2. 实现 PropertySource、Environment 和基础类型转换
3. 增加事件、监听器和同步发布器
4. 集成到 ApplicationContext 启动流程并发布 v0.10.0

验收标准：

- 可以从 classpath 和文件系统读取资源
- 配置属性可以按优先级查询并转换为常用类型
- 事件只分发给匹配监听器
- 环境、资源和事件具有独立单元测试与上下文集成测试

## 依赖关系

```text
构造器注入与依赖选择
→ 生命周期与 BeanPostProcessor
→ 三级缓存与早期引用
→ 自动代理与 AOP
→ Environment、资源与事件
```

生命周期扩展点是自动代理的基础；三级缓存需要能够保存早期代理工厂，因此应在 AOP 前完成。

## 每课执行流程

每一课都遵循同一套 TDD 闭环：

1. 明确一个可验收的行为和边界
2. 先写测试并确认因目标能力缺失而变红
3. 使用最小实现让目标测试变绿
4. 运行完整测试，确认没有回归
5. 重构命名、职责和重复逻辑
6. 更新 README、ROADMAP、CHANGELOG 和相关 Javadoc
7. 核对版本、Git 状态；达到里程碑时创建版本标签

## 下一步

下一课继续 v0.6.0：统一字段与构造器参数的依赖描述和候选解析边界，
消除两个注入点解析方法中的重复 qualifier/type 分支。
